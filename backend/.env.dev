# Development Environment Variables for Order Receiver Backend
# Copy this file to .env and update with your actual values

# Environment
ENVIRONMENT=dev

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=order-receiver-businesses-dev

# AWS Configuration (will be auto-detected from AWS CLI)
AWS_DEFAULT_REGION=us-east-1

# Cognito Configuration (Required for Cognito authentication)
# To get these values:
# 1. Go to AWS Console > Cognito User Pools
# 2. Create a User Pool or use existing one
# 3. Copy the User Pool ID and Client ID here
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=

# Local Development Settings
DEBUG=true
LOG_LEVEL=INFO

# API Configuration
API_VERSION=v1
CORS_ORIGINS=*
