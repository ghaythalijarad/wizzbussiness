# Lambda function dependencies - Pure serverless, no FastAPI
boto3>=1.34.0
botocore>=1.39.0

# For local development and testing
python-dotenv>=1.0.0

# Email validation (lightweight)
email-validator>=2.0.0

# JSON Web Token handling (if needed for custom auth)
pyjwt>=2.8.0

# AWS Lambda Powertools (recommended for production)
aws-lambda-powertools>=2.0.0

# HTTP utilities for testing
requests>=2.28.0

# For development/testing only
pytest>=7.0.0
moto>=4.2.0
