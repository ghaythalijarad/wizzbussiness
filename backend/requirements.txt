# MIGRATION NOTICE:
# This project has been migrated from FastAPI to AWS Lambda functions
# For serverless deployment, use: lambda_functions/requirements-lambda.txt
# This file is kept for local development and testing compatibility

# Legacy FastAPI dependencies (deprecated in favor of Lambda)
# fastapi==0.111.0
# uvicorn[standard]==0.29.0
# pydantic>=2.0,<3.0
# pydantic[email]>=2.0,<3.0

# Core dependencies (shared between legacy and serverless)
python-dotenv==1.0.1
boto3>=1.34.0

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
moto>=4.2.0
requests>=2.28.0

# Additional dependencies for Lambda testing
email-validator>=2.0.0
